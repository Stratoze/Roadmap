# Obsidian app state (volatile, machine-specific) - not tracked
.obsidian/

# Python
Mechatronics/.pytest_cache/
Mechatronics/simulations/python/.venv/
.ipynb_checkpoints/
*.pyc
__pycache__/

# OS cruft
.DS_Store
Thumbs.db

# Heavy binaries live in project repos / external storage, NOT the learning vault.
# Images in docs/captures/ stay tracked - they ARE the evidence.
*.step
*.stp
*.stl
*.3mf
*.gbr
*.gerber
*.drl
*.zip

# Local state / backups / plugin data
.backup_*/
session_state.json
.clipper/
# Reading full library is local-only; the tracked Reading/README.md skeleton keeps clones navigable
Reading/Reading RoadMap.md
# Learner's local scratch space, local-only by its own name. Holds a .venv:
# ~9.9k files / 94 MB. Never commit it.
Python For Testing Stuff, user only/

*.bak
*.pre-batch*

# Private companion repo (verbatim learner text; separate private remote)
_private/
