# Lessons

Lesson notes land here, one folder per topic: `<topic>/YYYY-MM-DD-<slug>.md`.
Created lazily by the `study` skill on the first lesson — never prefetched.
Summaries only; raw learner text lives in `_private/learning/verbatim/`.
