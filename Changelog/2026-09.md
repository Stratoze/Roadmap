# Changelog — 2026-09 (agent activity log; dailies are the learner's own)

## 2026-09-07 — Phase-1 rituals build + remote merge

- Phase-1 per `.opencode/plan/phase-1-agent-and-rituals.md`: catch-up commits
  (`219db87` Daily/2026-09-05, `e7b97f7` plans), 8 backfill stubs (08-29→09-04, 09-06),
  AGENT.md (58→82 lines), Daily Template 8 domain tokens, README AGENT link.
- Verification: session-start/due live (1 due: japanese-output kana); diagnose at
  baseline; 3 verifier briefs → fixes (`821a9f5`); EXEMPT block added.
- Remote: fetched origin (exactly 10 commits over `c1142a5`, tip `4b95ab4`);
  `engram/coordination` mailbox read (Win vault-scope plan, P0 pastes request).
- P0 pastes relayed: no engram plugin on Mac (FLAG vs 1.15.1), doctor 6/103 ok:true,
  sha256 `8c750572…`, ENGRAM_HOME proven at CLI (skill leg partial).
- Merge origin/main locally (`c65bad9` + EXEMPT fix `d30f5de`): AGENT/scripts/diagnose
  ours, template theirs+tokens, 08-30 theirs, 08-28 ours + parked claim, rest theirs.
  Post-merge diagnose: 1 broken (EXEMPT) + 0 orphans.
- Pushes blocked twice: local hook (Win's 10 unsigned) then server ruleset on main.
  Result: `mac/phase-1-rituals` @ `821a9f5`, `mac/merge-main` @ `365992b`,
  `mac/merge-reply` @ `cf42c83`. Main needs a PR.
- Learner Gap (verbatim): "i spent all that time changing the architecture of the
  code base and forgot to do today's engram"

## 2026-09-08 — Win re-sign landed, verified, branches abandoned

- Remote `main` moved `4b95ab4` → `baab4a6` (merge of `mac/merge-main-signed`);
  SHA map posted on `engram/coordination` (`88b5a8d`). Verified before accepting:
  tip tree byte-identical to our `b2f4669` (empty diff); merge message preserved
  verbatim on `c5e787e` incl. parked 08-28 text (grep=1); authors preserved
  (same email); no new tags; landed on protected main = server-verified signatures.
- Local `main` reset to `origin/main` (`baab4a6`); diagnose: 1 broken (EXEMPT) +
  0 orphans. Local `mac/*` branches deleted (content fully in main or on origin).
- Standing by: Win cuts `engram/vault-scoped-proposal` next; Mac idle per mailbox.

## 2026-09-08 — proposal-branch reconciliation jobs done, pushed back

- Read `engram/vault-scoped-proposal` job list (`MAC_NOTES.md` on branch).
- Exporter run on rebuild: Mac 6/103 vs Win 12/256; shared IDs are capstones-only
  with divergent hashes (near-miss queue, never auto-match).
- Dispositions (`MAC_DISPOSITIONS.jsonl`): 340 HOLD + 14 PORT (mech-spine) + 0 DROP.
- Engine sha FULL match (`8c750572…40aa546` both sides); ENGRAM_HOME vault-copy proof
  green (12/256 readable, vault untouched).
- ROADMAP Completion rewritten to 2-arg script reality (was false 3-arg text).
- Pushed to `engram/vault-scoped-proposal` @ `2448fe6` (clean push, signed commits).
  Diagnose on branch: 1 broken (EXEMPT) + 1 orphan (transient branch mail, by design).

## 2026-09-08 — Win Phase-2 feedback answered, amendments applied

- Win UPDATE (4): A–F review + 6 questions (G1–G6). Verified E-claims locally
  (all correct: 599 lines, heading levels, DS :9/:13, QDD @0.10:568, zero `#fragments`).
- User calls: QDD 2-DOF confirmed + keep `mech-spine` (rec); 08-28 fill DROPPED
  (unverifiable authorship, never-invent).
- Phase-2 plan amended (`f1a34a9`): §5 v4-gating + universe marking + sha-compared
  doctor; §4-before-text ordering, doc-governs-script; engine-fact corrections;
  taggerdate clocks + lightweight refusal + grandfathering; citation fixes;
  §1 scaffold-first; §6 ladder + authorship-verify + batch approval.
- G answers relayed on `mac/merge-reply` @ `a2e6a4e`. Awaiting proposal branch.
- Phase-2 plan APPROVED 2026-09-08 by user. Path: R20 → debloat round 3 (40 cuts, verifiers
  CLEAN ×2, diagnose green) → vault-goal principles encoded (ROADMAP spine, How-to-Learn
  boundary, plan §5) → R21 trivial fixes (receipt HEAD union, touched-set dedup, anchor
  clarifier, mechanically verified). No full 4-brief round on final one-liners (trivial per
  flow rule). Implementable; Phase-2 begins on next session.
- Sync-back rationale (durable record): scoped landing chosen over full merge because the
  Mac line's stale base would have wiped `_system/engram/` — see coordination `22cfd71`.
- Pending: Mac abandons stale branches; then delete `engram/coordination` mailbox (close-out).
- Variance (self-audit 2026-09-08): §3 registry + §6 lenses landed before §4 gate / §2 split
  (§7 order). No gated mint occurred, so no gate was evaded; order still binds all future mints.
- Convention (self-audit): review-session graph writes (FSRS schedule state) commit separately
  from content edits — `8af99c9` bundled them; not repeated.

## 2026-09-11 — Engram retirement; vault learning system live

- System: `.opencode/plan/vault-learning-system.md` (v3, 3 review rounds, round 3
  CLEAN). Skills `study`/`map`/`resources`/`review`; agents `scout`/`verifier`/
  `assessor`; transparent ladder `scripts/review.py`; curriculum model in
  `_system/learning/`; topic tree + theory link web in
  `_system/learning/topic-tree.md`.
- Salvaged: preferences only (`_system/learning/learner.md` + OpenViking
  `learning-preferences` memory; old `engram-standing-orders` memory deleted).
- Deleted: `_system/engram/` (store, graphs, artifacts, receipts); global
  install (skills, agents, engine, docs, experiments, commands, gold, smudge
  filters, backups, plugin entry); stale engram/mac branches.
- Updated: `AGENT.md` (session-start block; self-contained), How to Learn (SRS),
  README (Amos Blomqvist citation), index + domain files, templates,
  `.gitattributes`/`.gitignore`.
- Entry points: `/study`, `/map`, `/resources`, `/review` - thin project
  commands in `.opencode/command/` that load the matching skill (skills stay
  model-invoked; the wrappers put them in the slash catalog).
- Verification: `python3 scripts/diagnose.py` 0 broken / 0 orphans;
  `review.py selftest` ok; vault clean of operative engram references
  (Changelog/Daily history and the verbatim commitment cue excepted); global
  config grep clean.
- Intake (user-found gap, same day): pre-system topics had no path into the
  queue (rows came only from `study` teaching; `map` never moves state). `study`
  gains an Intake mode - compressed cold verifies -> schedule passes (clean
  rung 2, effortful rung 1), teach misses; 0.1/0.2 enter the queue this way.
- Contract fix (user): logistics/sequencing are agent-side - the learner is
  never asked to choose topics or run routine commands; `/study` is new-topic
  intent only (plan §2.10, AGENT.md, study skill).
- Protocol hardening (user): probing is ONE adaptive question per message -
  ask, wait, adapt; never batch or sweep. Hard rules in the map skill, AGENT.md,
  the /map command, and plan §3.2; review = one item per message.
- Quiz construction ported (was in the port list, never landed):
  `_system/learning/quiz-protocol.md`, referenced by map/study.
- Naming (user): what is next is told by category + content + link (math /
  physics / EE / ME / SW / method), never bare milestone ids; ids stay internal
  (AGENT.md, study close). Overview map opened at
  `_system/learning/overview-map.md` (strand 1: math - calculus).
- Flow audit (user): study loop + plan §2.2 now state the default video-first
  modality and premise-challenging explicitly; the resource pipeline
  (scout -> verifier) and reflection were already in the study skill.
- Daily ritual (user): Target + Predict / Got + Gap wired into the study skill
  (was AGENT.md-only); note duties restated (format learner words, never
  invent, never pre-fill Gap, agent activity -> Changelog).
- Mac: user removes the clone and engram directly; no agent steps.

## 2026-09-21 — DSH-only restructure + friction cuts (learner-directed)

- Harness: `.dsh/skills/{study,map,resources,review}/` + `.dsh/agents/{scout,verifier,assessor}.md`
  are the single live home (DSH convention, cf. World Friendship). Legacy
  `.agents/skills/`, `.opencode/` (agents + commands), `.codex/` frozen in
  `_system/learning/archive/harness-opencode|harness-codex/`; Codex
  `openai.yaml` metadata deleted; `slash: false` dropped from skill frontmatter.
  Skills invoke in plain language by either party - no slash infra.
- Learning system: `curriculum/odes.md` -> `curriculum/math-odes.md` (also fixed
  a `|`-in-cell parse bug on c6 that warned on every `due` run);
  `overview-map.md` -> `maps/overview.md`; `.opencode/plan/*` -> `archive/`;
  `archive/` + `lessons/` READMEs added. Plans are history, never operative.
- Entries: `index.md` is the human map (role header), `AGENTS.md` the contract,
  `README.md` the face. Domain hubs mark not-yet-created curriculum files as
  proposed. `Reading/README.md` tracked skeleton added (full `Reading RoadMap`
  stays local-only; `.gitignore` narrowed from `Reading/` to the file).
- Daily friction cut (learner: bureaucracy-first kills momentum): no
  Target+Predict gate, missing notes never nagged; template
  `_system/Daily Template.md` -> `_templates/daily.md` rewritten as a
  post-session log the agent drafts from evidence and the learner corrects.
  Gates removed from map/study skills; `AGENTS.md` session-start/end, Got/Gap,
  and hard-rule lines rewritten.
- Focus queue (learner, 2026-09-21): math -> physics -> electronics into
  mechatronics; JP grammar + data science alongside; piano maintenance-only
  (JP >= mech >>> piano). Japanese tracks opened: `japanese-grammar`,
  `japanese-output`, `japanese-reading`, `japanese-immersion` (L1->L3 ramp to
  all-Japanese; concepts map at first contact).
- Cleanup: 161 `.obsidian/workspace*.json` snapshots deleted (2 kept);
  `Daily/Index.md` stale break banner removed; `_templates/Index.md` gains a
  mech phase->template table; `scripts/README.md` added; `diagnose.py` drops
  the Reading EXEMPT, excludes `archive/` from broken-link reports and
  system-managed dirs from orphan reports.
- Verification: `diagnose.py` 0 broken / 0 orphans (no EXEMPT);
  `review.py selftest` ok, `due` clean. Stale-ref grep: only archive history.

## 2026-09-22 — First math-ODEs study session; learner-corrected close

- Session: ODE orientation via the cooling rule. Learner wrote the Euler update
  loop unaided (sign bug surfaced by running it: 90 -> 140.3 and no
  termination), then the gap form, the algebraic fixed point, a frozen
  30-minute step (-57 — the method breaks, not the rule), the convergence
  table, growth vs decay, and stable vs unstable equilibria.
- Recorded: `curriculum/math-odes.md` gains evidence on c1/c4/c5/c6/c7 (all
  scheduled rung 0), two 2026-09-22 misconceptions, six problem rows p1-p6, the
  lesson link, and a log entry. New lesson note
  `lessons/math-odes/2026-09-22-cooling-rule-and-euler.md`; Daily
  `2026-09-22.md` drafted for learner correction.
- Procedure: learner rejected an e introduced without derivation (correct —
  separation of variables is deferred by the plan) and rejected a blank-page
  test that only re-ran a pre-existing skill (writing a loop). Test changed to
  an unseen rule, which exposed a real gap. Both corrections applied.
- Agent error: attributed 0.1134 to the learner; it was the agent's own
  batch-gap figure. The learner's -0.88663 is the same fact from the other
  side. Corrected in-session.
- Blocker: `_private/` not cloned -> verbatim and confidence capture paused;
  public summaries only.
- Toolchain drift: this machine's venv python is 3.13.5 (AGENTS.md facts say
  3.13.11). Recorded, not yet reconciled.
- Notebook tooling (learner-reported): `.ipynb` hangs at "Connecting to kernel
  .venv (3.13.5.final.0)" while plain python runs fine. Ruled out by execution:
  the `.venv` python spawns, `ipykernel_launcher` exits 0, zmq 27.2.0 /
  jupyter_client 8.10.0 / tornado 6.5.10 present, `pip list` takes 0.6 s,
  `%APPDATA%\jupyter\runtime` exists, notebook JSON valid. `nbformat` 5.11.1 and
  `nbclient` 0.11.0 were installed into the `.venv` anyway (they were missing;
  not the cause — an earlier agent guess that they were is retracted).
  Real finding, from the Jupyter output log: VS Code never reaches the kernel
  launch — it stalls in environment resolution after `pip list`, with
  `getExecutablesPath` erroring `USER_SITE not found`. The Jupyter extension is
  2025.9.1 against VS Code 1.138.0 / ms-python.python 2026.4.0 /
  vscode-python-envs 1.36.0, and it is the only version installed. CLI
  `--install-extension --force` refused to upgrade ("already installed",
  version unchanged); no `extensions.autoUpdate` override in user settings.
  Update left to the learner via the UI.
- PATH landmine (found, not fixed): `~\.local\bin\python3.10.exe` is a uv
  trampoline whose target is gone (`entity not found (os error 2)`); it errors
  on every environment resolution. `python3.13.exe` beside it untested.
- Commitment cue retired (learner, verbatim: "retire it, engram is gone"):
  `learner.md` drops the 2026-08-21 cue "lunch or the afternoon tomorrow" ->
  "clear the engram reviews". The engram system it named was deleted
  2026-09-11. No cue is active; a new one is stored only when the learner
  offers one in their own words.
- CORRECTION (agent error, same session): the close recorded "`_private/` not
  cloned -> verbatim capture paused" in the Daily Sticky, the lesson note, and
  the curriculum log. That was FALSE. `_private/.git` is a real directory and
  the private repo is clean and in sync; the check that reported it missing was
  a git-bash `test` that crashed under the agent sandbox and fell through to its
  `||` branch. The three false claims are corrected, and 2026-09-22 verbatim was
  written to `_private/learning/verbatim/2026-09-22-odes-euler.md`. Lesson for
  this environment: a crashed shell check means UNKNOWN, never false.
- `.gitignore`: `Python For Testing Stuff, user only/` added. It is 9,922 files
  / 93.7 MB, almost all a `.venv`, and it was untracked — so a bare
  `git add -A` would have committed it.

## 2026-09-22 — Mac/Windows merge reconciliation (books-over-videos wins)

- Divergence: `origin/main` held 10 Mac commits (2026-09-14: Japanese grammar
  strand 1 taught — e-row potential, misconception logged, 2 concepts
  scheduled; `japanese-grammar` / `japanese-output` / `python` curriculum files;
  Phase 0 `m0-1`..`m0-10`; `00_foundations.md`; Mac toolchain facts) against 5
  local commits (AGENTS.md rename, learner.md doctrine, Codex port, DSH-only
  restructure, the 2026-09-22 math-odes session).
- Structure held: rename detection carried the Mac's `.opencode/` skill and
  agent edits straight into `.dsh/`, so the DSH-only restructure was NOT undone
  and no live `.opencode/` tree was resurrected. The feared "stale Mac base" trap
  did not fire.
- Conflicts resolved: `AGENTS.md` (ours — `maps/overview.md`, focus queue, no
  slash commands; their new "Source-first lessons" section kept), and
  `japanese-grammar.md` + `japanese-output.md` (their concepts / resources /
  misconceptions / problems / lessons, plus our 2026-09-21 L1->L3 ramp and
  pacing lines).
- Doctrine conflict (the substantive one): local `learner.md` carried
  **video-first modality**; the Mac line carried **read-first** +
  **source-first** + **books over videos** (2026-09-15). The learner decided
  2026-09-22, verbatim: "books win, for video i can just search them myself, for
  books you probably know more books than any human do". Standing orders are now
  1-10: 5 read-first, 8 source-first, 9 books over videos, 10 duo-track
  terminology.
- Pushed: vault `853ce6b..acec53a`; private `955d17f..9f66fcf`. Diagnose after
  the merge: 0 broken / 0 orphans.
- Note: `bash scripts/save.sh` cannot run in the agent sandbox (git-bash dies
  with a `CreateFileMapping` fault), so the commit was made with the equivalent
  `git add -A` + `git commit`, with save.sh's `_private/` guard run explicitly
  first.

## 2026-09-22 (late) — JP backlog review; handoff resumed

- Session opened by executing the handoff `20260922-212927-odes-session-merge.md`.
  State verified live before acting (not assumed): both repos clean and in sync,
  `_private/.git` present, `git check-ignore -q _private/` exit 0, `diagnose.py`
  0 broken / 0 orphans, today's Daily exists.
- Queue check corrected the handoff: `review.py due` returned only the two
  Japanese items (overdue since 2026-09-15). The math-odes items are due
  2026-09-23 and were deliberately NOT probed tonight.
- `jp-godan-masu` → `hard`. All three forms correct (のみます/かきます/はなします)
  and the rule right in substance, but the canonical term was missing at recall
  ("standard term ? whats that") — understanding hit, articulacy miss per
  standing order 10. Term recorded for re-probe.
- `jp-godan-potential` → `miss`, rung 0. 2/5 (かける, たべられる correct;
  まつ→ませる, よぶ unchanged, する unanswered). **Second lapse, identical root
  cause to 2026-09-14**: the t-row took the s-row's e column. Flagged as a
  grid-lookup failure, not a memory lapse. The learner's objection that よべる
  "sounds cursed" was accepted as a genuine frequency observation (b-row/m-row
  potentials are regular but comparatively rare), not corrected as an error.
- Relearn switched encoding to a gojuuon-grid lookup per the review skill's
  two-lapse rule. **Left unfinished** — the learner closed to continue on the
  Mac. Recorded as unconfirmed so the next session does not treat it as settled.
- Physics: learner asked to continue with physics after the JP backlog. No
  physics curriculum file exists (`Science/Index.md` holds only the proposed
  `physics-first-principles` stub, `[decompose at activation]`), so it opens as
  a new topic — scoping question, scout field scan, prune. Not started; the
  session closed first. Deferred, not dropped.
- ODE book re-sourcing level agreed with the learner: beginner/applied,
  zero-schema safe. The existing dossier is video/lecture-heavy and therefore
  non-compliant with standing order 9; the fix is queued for the ODE leg.
- Process note: one bad `edit` on `japanese-grammar.md` deleted the `## Problems`
  table; caught by re-reading and restored with p1/p2 plus the new p3. Recorded
  because it is exactly the "verify before claiming" failure the contract warns
  about.
- Uncommitted `.scratch/` (handoff docs) raised with the learner rather than
  committed or ignored unilaterally — it is not in `.gitignore`.
